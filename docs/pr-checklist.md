Code Health (Functional Correctness):

We created a pull request template file (.github/pull_request_template.md) to ensure that each change addresses a specific issue.

We updated the README.md file to ensure that the written instructions match the actual way the code runs.

Test Coverage:

We made sure there is a pr-checklist.md file in the docs folder that asks developers to verify tests before submitting code.

Documentation:

We edited the README.md file to add a "How to run" section to make the documentation accurate and up-to-date.

Scope:

We added a step in the PR template that asks developers to specify a summary of changes (What changed?) to ensure that each PR covers only one thing.

No Debugging Noise:

We added an explicit item in the self-review checklist (pr-checklist.md) that asks to remove temporary print statements and breakpoint() before submitting the code.